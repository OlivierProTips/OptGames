services:
  ctfgames:
    build: .
    user: 1000:1000
    container_name: ctfgames
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/assets
    environment:
      - FLASK_ENV=production
      - DOCKER_URL=localhost
    restart: unless-stopped
